<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!--
    Повторный вызов настроек — уже после того, как каталог людей
    объявлен. При первой установке coop_base грузится раньше, и
    домашнего экрана тогда ещё не существует. Вызов идемпотентный.
  -->
  <function model="coop.setup" name="apply"/>
</odoo>
