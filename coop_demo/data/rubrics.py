# -*- coding: utf-8 -*-
"""Рубрика по названию — для объявлений, заведённых из потребностей.

Зачем. Проект объявляет, что ему нужно: разнорабочие на площадку,
перевозка материалов, помещение под хранение. Из каждой такой
потребности заводится объявление в каталоге ресурсов или вакансия — но
загрузчик потребностей рубрику им не ставил, и в каталогах это две с
половиной сотни записей без раздела. На витрине они все свалились в
полку «Другое», а в отборе по рубрике не находились вовсе.

Правило то же, что и со снимками: **всё, что определяется названием, из
названия и выводится**. Ключи взяты из того, что в каталогах
действительно лежит, и порядок в них значим — от частного к общему.
"""

# Название объявления → рубрика каталога ресурсов.
RESOURCES = [
    (('перевозк', 'доставк', 'вывоз', 'самосвал', 'грузовик'),
     'Грузовики и спецтехника'),
    (('помещение', 'хранилищ', 'склад', 'ангар', 'площадк под'),
     'Склады и коммерческая недвижимость'),
    (('сервер', 'дата-центр', 'стойко-место', 'сетевое оборудование',
      'коммутатор', 'маршрутизатор', 'носител ключ', 'токены и'),
     'Ноутбуки и компьютеры'),
    (('камера и свет', 'микрофон', 'звук', 'видеокамер'),
     'Видео и аудио'),
    (('станок', 'печь', 'вытяжк', 'вентиляц', 'пресс', 'смесител',
      'холодильн', 'морозильн', 'зарядная станц', 'солнечные панел',
      'капельн', 'инкубатор', 'сепаратор'),
     'Оборудование для бизнеса'),
    (('стеллаж', 'столы', 'стулья', 'мебель', 'читальн'),
     'Для дома и дачи'),
    (('улья', 'ульи', 'вощин', 'рамк', 'медогонк'),
     'Мёд и продукты пчеловодства'),
    (('молок', 'сыр', 'творог', 'мясн'), 'Мясная и молочная продукция'),
    (('мука', 'зерн', 'крупа', 'комбикорм'), 'Крупы, мука и бакалея'),
    (('рассада', 'семена', 'саженц', 'овощ', 'картофел'),
     'Овощи и фрукты'),
    (('бетон', 'опалубк', 'арматур', 'кирпич', 'цемент', 'ворота',
      'покрытие', 'кровл', 'утеплител', 'пиломатериал', 'доск', 'брус',
      'игровой комплекс'),
     'Ремонт и стройматериалы'),
    (('инструмент', 'дрель', 'перфоратор'), 'Инструменты'),
    (('грант', 'заём', 'займ', 'инвестиц', 'кредит', 'пожертвован'),
     'Финансирование'),
    (('сопровожден', 'консультац', 'услуг', 'подряд', 'проверк'),
     'Деловые услуги'),
]

# Название вакансии → специализация из общего справочника. Сфера
# деятельности — рубрика каталога — считается от неё сама.
VACANCIES = [
    (('разнорабоч', 'подготовка площадк', 'подсобн'), 'Разнорабочий'),
    (('бухгалтер', 'первичк', 'учёт'), 'Бухгалтерия'),
    (('юрист', 'юридическ', 'лицензи на по', 'правов'), 'Юрисконсульт'),
    (('ревизия кода', 'разработчик', 'программист', 'интеграци',
      'криптограф', 'зависимост', 'odoo', 'мобильн'),
     'Программирование, Разработка'),
    (('тестирован', 'проверка протокол', 'протокол подсчёт'), 'Тестирование'),
    (('аналитик', 'data'), 'Аналитика, Data Science'),
    (('дизайнер', 'интерфейс', 'график'), 'Дизайн, графика'),
    (('монтаж', 'фото', 'видео', 'оператор печатн'),
     'Фото, видео, звукооператоры'),
    (('сыровар', 'пекар', 'повар', 'кондитер'), 'Повар, кондитер'),
    (('агроном', 'зоотехник'), 'Агроном, зоотехник'),
    (('пчелов', 'животновод'), 'Пчеловодство, животноводство'),
    (('тракторист', 'механизатор'), 'Тракторист'),
    (('сварщик',), 'Сварщик'),
    (('столяр', 'плотник'), 'Столяр, плотник'),
    (('электрик', 'электромонт'),
     'Электромонтажник, электромонтер, техник-электрик'),
    (('слесар', 'сантехник'), 'Слесарь, сантехник'),
    (('бетонщик', 'арматурщик', 'каменщик'), 'Бетонщик, арматурщик, каменщик'),
    (('прораб', 'мастер смр'), 'Прораб, мастер СМР'),
    (('кладовщик', 'приёмщик', 'приемщик'), 'Кладовщик, Приемщик товаров'),
    (('библиотекар', 'преподавател', 'курс', 'обучен'),
     'Преподаватель, репетитор'),
    (('переводчик',), 'Переводчик'),
    (('менеджер заказ', 'менеджер по продаж', 'продаж'),
     'Менеджер по продажам'),
    (('закупк',), 'Менеджер по закупкам'),
    (('безопасност', 'охранник', 'оповещен'), 'Охранник, контролёр'),
    (('швея', 'закройщик'), 'Швея, закройщик'),
    (('руководител проект', 'координатор', 'ведущий разбора'),
     'Руководитель проектов'),
    (('аудитор', 'аудит', 'смарт-контракт', 'повторная проверка',
      'разбор попыток'),
     'Тестирование'),
    (('инженер по эксплуатац', 'дежурный инженер', 'эксплуатац'),
     'Инженер-технолог'),
    (('редактор', 'технический писател', 'писател'), 'Журналистика, СМИ'),
    # Последним — сеть, в которую попадает всё, чему не нашлось места.
    # Пустая специализация хуже: такая вакансия не находится отбором и
    # уходит на витрине в полку «Другое» вместе с теми, у кого рубрики
    # нет вовсе, а это разные вещи.
    (('',), 'Разное'),
]


def _by_title(rules, name):
    bottom = (name or '').lower()
    for keys, value in rules:
        if any(key in bottom for key in keys):
            return value
    return None


def category_for(name):
    """Рубрика каталога ресурсов по названию объявления, или пусто."""
    return _by_title(RESOURCES, name)


def specialization_for(name):
    """Специализация по названию вакансии, или пусто."""
    return _by_title(VACANCIES, name)


def merge_duplicate_categories(env):
    """Свести рубрики-двойники в одну.

    В справочнике оказались «Ремонт и стройматериалы», «Инструменты» и
    «Велосипеды» по два раза: одну завела выгрузка макета, другую —
    загрузчик ресурсов по своей карте родителей. На витрине это видно
    сразу: две полки с одним названием, и записи поделены между ними
    поровну.

    Оставляем ту, что стоит в дереве по карте загрузчика
    (`CATEGORY_PARENTS`), — она и есть намеренная. Записи двойника
    переносим, вложенные рубрики перецепляем, двойника удаляем.

    Возвращает, сколько двойников свели.
    """
    from . import load_resources

    Category = env['coop.resource.category'].sudo()
    all_items = Category.search([])
    by_names = {}
    for rubric in all_items:
        by_names.setdefault(rubric.name, []).append(rubric)

    merged = 0
    for name, dupes in by_names.items():
        if len(dupes) < 2:
            continue
        parent = load_resources.CATEGORY_PARENTS.get(name)
        def fits(r):
            if parent:
                return r.parent_id.name == parent
            return not r.parent_id
        # Если по карте не выбрать — оставляем ту, где больше записей:
        # меньше переносить и меньше шансов задеть чужие ссылки.
        main_one = next((r for r in dupes if fits(r)), None)
        if main_one is None:
            main_one = max(dupes, key=lambda r: len(r.resource_ids)
                          if 'resource_ids' in r._fields else 0)
        for dupe in dupes:
            if dupe == main_one:
                continue
            env['coop.resource'].sudo().search(
                [('category_id', '=', dupe.id)]).write(
                    {'category_id': main_one.id})
            Category.search([('parent_id', '=', dupe.id)]).write(
                {'parent_id': main_one.id})
            # Назначения характеристик уникальны парой «характеристика
            # плюс рубрика»: у главной такая пара уже может быть, и
            # перенос падает на ограничении. Совпавшие просто удаляем —
            # они означают ровно то же самое.
            if 'coop.attribute.assignment' in env:
                Assignment = env['coop.attribute.assignment'].sudo()
                taken_list = set(Assignment.search(
                    [('category_id', '=', main_one.id)]).mapped('attribute_id').ids)
                for purpose in Assignment.search(
                        [('category_id', '=', dupe.id)]):
                    if purpose.attribute_id.id in taken_list:
                        purpose.unlink()
                    else:
                        purpose.category_id = main_one.id
                        taken_list.add(purpose.attribute_id.id)
            for model, field in (('coop.token.claim', 'resource_category_id'),
                                 ('coop.token.order', 'resource_category_id')):
                if model not in env:
                    continue
                # Связанные поля пересчитаются сами — трогаем только те,
                # что хранят свой выбор.
                model_field = env[model]._fields.get(field)
                if model_field is None or model_field.related:
                    continue
                env[model].sudo().search([(field, '=', dupe.id)]).write(
                    {field: main_one.id})
            dupe.unlink()
            merged += 1
    return merged


def fill_resources(env):
    """Проставить рубрику объявлениям, у которых её нет.

    Идёт по тем, у кого пусто, и потому безвреден при повторном запуске.
    Возвращает, скольким поставили и скольким не нашлось: второе важнее —
    по нему видно, каких правил не хватает.
    """
    Category = env['coop.resource.category'].sudo()
    cache = {}
    placed = not_found = 0
    for record in env['coop.resource'].sudo().search(
            [('category_id', '=', False)]):
        name = category_for(record.name)
        rubric = None
        if name:
            if name not in cache:
                cache[name] = Category.search([('name', '=', name)], limit=1)
            rubric = cache[name]
        if not rubric:
            not_found += 1
            continue
        record.write({'category_id': rubric.id})
        placed += 1
    return placed, not_found


def fill_vacancies(env):
    """Проставить специализацию вакансиям, у которых её нет.

    Сфера деятельности ставится не отдельно: поле связанное и считается
    от специализации само. Ставить обе руками значило бы завести вторую
    точку правды, которая разойдётся с первой.
    """
    if 'coop.vacancy' not in env:
        return 0, 0
    Spec = env['coop.specialization'].sudo()
    cache = {}
    placed = not_found = 0
    for record in env['coop.vacancy'].sudo().search(
            [('coop_specialization_id', '=', False)]):
        name = specialization_for(record.name)
        spec = None
        if name:
            if name not in cache:
                cache[name] = Spec.search([('name', '=', name)], limit=1)
            spec = cache[name]
        if not spec:
            not_found += 1
            continue
        record.write({'coop_specialization_id': spec.id})
        placed += 1
    return placed, not_found
