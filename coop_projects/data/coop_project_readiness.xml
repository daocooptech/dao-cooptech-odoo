<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!--
    Потолок готовности в сто процентов снят (пункт 12 разбора
    архитектора). Хранимое поле от правки формулы само не
    пересчитывается, поэтому зовём пересчёт при обновлении модуля.
    Вызов идемпотентный: если обрезанных нет, он ничего не делает.
  -->
  <function model="coop.project" name="recompute_readiness"/>
</odoo>
